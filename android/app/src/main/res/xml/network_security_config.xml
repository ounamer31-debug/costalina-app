<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security policy.
  - HTTPS is required everywhere by default.
  - Cleartext (HTTP) is allowed ONLY for localhost / loopback, which is how the
    app talks to a dev backend via `adb reverse tcp:3000 tcp:3000`.
  - 10.0.2.2 is the Android emulator alias for the host machine.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>